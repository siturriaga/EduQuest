# Netlify configuration for static HTML deployment
# No build command needed - just serve the files

[build]
  publish = "."

# Redirect all routes to index.html for SPA behavior
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
